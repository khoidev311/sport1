export enum DataStatusEnum {
  ACTIVATED = "activated",
  DISABLED = "disabled",
}

export enum OrderDirectionEnum {
  ASC = "ASC",
  DESC = "DESC",
}

export enum SelectPositionEnum {
  TOP_LEFT = "top_left",
  TOP_RIGHT = "top_right",
  BOTTOM_LEFT = "bottom_left",
  BOTTOM_RIGHT = "bottom_right",
}

export enum TeamLogoAndNameEnum {
  LOGO_TO_NAME = "logo_to_name",
  NAME_TO_LOGO = "name_to_logo",
  LOGO_INTO_NAME = "logo_into_name",
}

export enum TabFeatureEnum {
  SCOREBOARD = "scoreboard",
  FIXTURES = "fixtures",
  RANKING = "ranking",
}

export enum FinishMatchEnum {
  WIN = "W",
  LOST = "L",
  DRAW = "D",
}

export enum NewsCategoryEnum {
  LASTEST = "lastest",
  BIG = "big",
  HOT = "hot",
  TRANSFER = "transfer",
}
export enum NewsItemLayoutEnum {
  ROW = "row",
  COLUMN = "column",
}
